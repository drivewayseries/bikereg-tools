# bikereg-registrations

Paste up to 20 BikeReg event IDs and get one verified CSV of every public registration.

See the repository README for install and usage. The script in `skills/bikereg-registrations/scripts/` also runs standalone with Python 3 (no dependencies).
